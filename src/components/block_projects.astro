---
// src/components/projects.astro
import ItemArrow from './item_arrow.astro';
---

<section class="projects">
	<div class="projects__inner container">
		
		<div class="split-layout">
			
			<div class="split-layout__sidebar">
				<div class="section-intro section-intro--projects">
					<div class="section-intro__line"></div>
					<h2 class="section-intro__title">PROJECTS & ACTIVITIES</h2>
					<p class="section-intro__text">
						We’ve helped to succesfully accomplished different projects.
					</p>
					<div class="section-intro__footer">
						<a href="#" class="section-intro__actions group" aria-label="See all projects">
							<span class="section-intro__link section-intro__link--arrow">&rarr;</span>
							<span class="section-intro__link">&gt; see all_</span>
						</a>
					</div>
				</div>
			</div>

			<div class="split-layout__content">
				<div class="carousel">
					<div class="carousel__viewport">
						<div class="carousel__track" id="projects-track">

						<article class="service-card group">
							<div class="service-card__tag">BIM</div>
							<div class="service-card__content">
								<h3 class="service-card__title">1BIM Consulting & Standards 1</h3>
								<p class="service-card__desc">
									We support ongoing projects by establishing and maintaining digital collaborative workflows among involved parties.
								</p>
							</div>
							<a href="#" class="service-card__link">
								<span class="service-card__link-text">&gt; details_&nbsp;</span>
								<span class="service-card__link-hover">&gt; explore_</span>
							</a>
							<div class="service-card__cutout">
								<button class="service-card__action" aria-label="Explore BIM Consulting">
									<ItemArrow />
								</button>
							</div>
						</article>

						<article class="service-card group">
							<div class="service-card__tag">BIM</div>
							<div class="service-card__content">
								<h3 class="service-card__title">BIM Management & Coordination 2</h3>
								<p class="service-card__desc">
									We support ongoing projects by establishing and maintaining digital collaborative workflows among involved parties.
								</p>
							</div>
							<a href="#" class="service-card__link">
								<span class="service-card__link-text">&gt; details_&nbsp;</span>
								<span class="service-card__link-hover">&gt; explore_</span>
							</a>
							<div class="service-card__cutout">
								<button class="service-card__action" aria-label="Explore BIM Consulting">
									<ItemArrow />
								</button>
							</div>
						</article>

						<article class="service-card group">
							<div class="service-card__tag">DEV</div>
							<div class="service-card__content">
								<h3 class="service-card__title">Development & Automation 3</h3>
								<p class="service-card__desc">
									We support ongoing projects by establishing and maintaining digital collaborative workflows among involved parties.
								</p>
							</div>
							<a href="#" class="service-card__link">
								<span class="service-card__link-text">&gt; details_&nbsp;</span>
								<span class="service-card__link-hover">&gt; explore_</span>
							</a>
							<div class="service-card__cutout">
								<button class="service-card__action" aria-label="Explore BIM Consulting">
									<ItemArrow />
								</button>
							</div>
						</article>

						<article class="service-card group">
							<div class="service-card__tag">BIM</div>
							<div class="service-card__content">
								<h3 class="service-card__title">BIM Management & Coordination 4</h3>
								<p class="service-card__desc">
									We support ongoing projects by establishing and maintaining digital collaborative workflows among involved parties.
								</p>
							</div>
							<a href="#" class="service-card__link">
								<span class="service-card__link-text">&gt; details_&nbsp;</span>
								<span class="service-card__link-hover">&gt; explore_</span>
							</a>
							<div class="service-card__cutout">
								<button class="service-card__action" aria-label="Explore BIM Consulting">
									<ItemArrow />
								</button>
							</div>
						</article>

						<article class="service-card group">
							<div class="service-card__tag">DEV</div>
							<div class="service-card__content">
								<h3 class="service-card__title">Development & Automation 5</h3>
								<p class="service-card__desc">
									We support ongoing projects by establishing and maintaining digital collaborative workflows among involved parties.
								</p>
							</div>
							<a href="#" class="service-card__link">
								<span class="service-card__link-text">&gt; details_&nbsp;</span>
								<span class="service-card__link-hover">&gt; explore_</span>
							</a>
							<div class="service-card__cutout">
								<button class="service-card__action" aria-label="Explore BIM Consulting">
									<ItemArrow />
								</button>
							</div>
						</article>

						<article class="service-card group">
							<div class="service-card__tag">DEV</div>
							<div class="service-card__content">
								<h3 class="service-card__title">Development & Automation 6</h3>
								<p class="service-card__desc">
									We support ongoing projects by establishing and maintaining digital collaborative workflows among involved parties.
								</p>
							</div>
							<a href="#" class="service-card__link">
								<span class="service-card__link-text">&gt; details_&nbsp;</span>
								<span class="service-card__link-hover">&gt; explore_</span>
							</a>
							<div class="service-card__cutout">
								<button class="service-card__action" aria-label="Explore BIM Consulting">
									<ItemArrow />
								</button>
							</div>
						</article>

						<article class="service-card group">
							<div class="service-card__tag">DEV</div>
							<div class="service-card__content">
								<h3 class="service-card__title">Development & Automation 7</h3>
								<p class="service-card__desc">
									We support ongoing projects by establishing and maintaining digital collaborative workflows among involved parties.
								</p>
							</div>
							<a href="#" class="service-card__link">
								<span class="service-card__link-text">&gt; details_&nbsp;</span>
								<span class="service-card__link-hover">&gt; explore_</span>
							</a>
							<div class="service-card__cutout">
								<button class="service-card__action" aria-label="Explore BIM Consulting">
									<ItemArrow />
								</button>
							</div>
						</article>

						<article class="service-card group">
							<div class="service-card__tag">DEV</div>
							<div class="service-card__content">
								<h3 class="service-card__title">Development & Automation 8</h3>
								<p class="service-card__desc">
									We support ongoing projects by establishing and maintaining digital collaborative workflows among involved parties.
								</p>
							</div>
							<a href="#" class="service-card__link">
								<span class="service-card__link-text">&gt; details_&nbsp;</span>
								<span class="service-card__link-hover">&gt; explore_</span>
							</a>
							<div class="service-card__cutout">
								<button class="service-card__action" aria-label="Explore BIM Consulting">
									<ItemArrow />
								</button>
							</div>
						</article>

						<article class="service-card group">
							<div class="service-card__tag">DEV</div>
							<div class="service-card__content">
								<h3 class="service-card__title">Development & Automation 9</h3>
								<p class="service-card__desc">
									We support ongoing projects by establishing and maintaining digital collaborative workflows among involved parties.
								</p>
							</div>
							<a href="#" class="service-card__link">
								<span class="service-card__link-text">&gt; details_&nbsp;</span>
								<span class="service-card__link-hover">&gt; explore_</span>
							</a>
							<div class="service-card__cutout">
								<button class="service-card__action" aria-label="Explore BIM Consulting">
									<ItemArrow />
								</button>
							</div>
						</article>

						</div>
					</div>
					<div class="carousel__pagination">
						<div class="carousel__indicators"></div>
						<div class="carousel__nav">
							<button type="button" class="carousel__nav-btn" aria-label="Previous slide">&larr;</button>
							<button type="button" class="carousel__nav-btn" aria-label="Next slide">&rarr;</button>
						</div>
					</div>
				</div>
			</div>

		</div>
		
	</div>
</section>

<!-- SLIDING -->

<script>
document.addEventListener('DOMContentLoaded', () => {
	// 1. We grab the parent section FIRST to scope our queries
	const section = document.querySelector('.projects') as HTMLElement;
	if (!section) return;

	// 2. Now we only look for elements INSIDE this specific section!
	const track = section.querySelector('.carousel__track') as HTMLElement;
	const cards = section.querySelectorAll('.service-card');
	const prevBtn = section.querySelector('.carousel__nav-btn[aria-label="Previous slide"]') as HTMLButtonElement;
	const nextBtn = section.querySelector('.carousel__nav-btn[aria-label="Next slide"]') as HTMLButtonElement;
	const indicatorsContainer = section.querySelector('.carousel__indicators') as HTMLElement;

	if (!track || cards.length === 0 || !indicatorsContainer) return;

	// make slashes
	indicatorsContainer.innerHTML = '';
	cards.forEach((_, i) => {
		const slash = document.createElement('button');
		slash.type = 'button';
		slash.className = 'carousel__indicator'; // <-- Updated class
		slash.textContent = '/';
		slash.setAttribute('aria-label', `Go to slide ${i + 1}`);
		indicatorsContainer.appendChild(slash);
	});

	const indicators = section.querySelectorAll('.carousel__indicator'); // <-- Updated class
	let currentIndex = 0;
	let maxIndex = 0;
	let autoSlideTimer: ReturnType<typeof setInterval>;

	function updateSlider() {
		const cardWidth = (cards[0] as HTMLElement).offsetWidth;
		const gap = 40; 
		const step = cardWidth + gap;

		const containerWidth = track.parentElement!.offsetWidth;
		const visibleCards = Math.floor((containerWidth + gap) / step) || 1;
		maxIndex = Math.max(0, cards.length - visibleCards);

		if (currentIndex > maxIndex) currentIndex = maxIndex;
		if (currentIndex < 0) currentIndex = 0;

		track.style.transform = `translateX(-${currentIndex * step}px)`;

		// highlight slashes
		indicators.forEach((slash, index) => {
			if (index < currentIndex + visibleCards) {
				slash.classList.add('carousel__indicator--active'); // <-- Updated class
			} else {
				slash.classList.remove('carousel__indicator--active'); // <-- Updated class
			}
		});

		if (prevBtn) {
			prevBtn.style.opacity = currentIndex === 0 ? '0.3' : '1';
			prevBtn.style.cursor = currentIndex === 0 ? 'default' : 'pointer';
		}
		if (nextBtn) {
			nextBtn.style.opacity = currentIndex === maxIndex ? '0.3' : '1';
			nextBtn.style.cursor = currentIndex === maxIndex ? 'default' : 'pointer';
		}
	}

	function startAutoSlide() {
		clearInterval(autoSlideTimer);
		autoSlideTimer = setInterval(() => {
			if (currentIndex === maxIndex) {
				currentIndex = 0;
			} else {
				currentIndex += 3;
				if (currentIndex > maxIndex) currentIndex = maxIndex;
			}
			updateSlider();
		}, 8500);
	}

	prevBtn?.addEventListener('click', () => {
		if (currentIndex > 0) {
			currentIndex--;
			updateSlider();
			startAutoSlide();
		}
	});

	nextBtn?.addEventListener('click', () => {
		const cardWidth = (cards[0] as HTMLElement).offsetWidth;
		const visibleCards = Math.floor((track.parentElement!.offsetWidth + 40) / (cardWidth + 40)) || 1;
		const maxIndex = Math.max(0, cards.length - visibleCards);

		if (currentIndex < maxIndex) {
			currentIndex++;
			updateSlider();
			startAutoSlide();
		}
	});

	// click slashes
	indicators.forEach((slash, index) => {
		slash.addEventListener('click', () => {
			const cardWidth = (cards[0] as HTMLElement).offsetWidth;
			const visibleCards = Math.floor((track.parentElement!.offsetWidth + 40) / (cardWidth + 40)) || 1;
			// calc the slides
			const targetIndex = index - visibleCards + 1;
			// safe boxes
			currentIndex = Math.max(0, Math.min(targetIndex, maxIndex));
			updateSlider();
			startAutoSlide();
		});
	});

	// pause on hover
	track.addEventListener('mouseenter', () => clearInterval(autoSlideTimer));
	track.addEventListener('mouseleave', startAutoSlide);

	window.addEventListener('resize', updateSlider);
	
	updateSlider();
	startAutoSlide();
});
</script>